<UserControl x:Class="SalaryManager.App.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="PageTabItem" TargetType="TabItem" BasedOn="{StaticResource MaterialDesignTabItem}">
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
        </Style>
        <Style x:Key="NeutralChecklistItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GridRowButton" TargetType="Button">
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinWidth" Value="44"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#94A3B8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GridRowDangerButton" TargetType="Button" BasedOn="{StaticResource GridRowButton}">
            <Setter Property="Background" Value="#FEF2F2"/>
            <Setter Property="BorderBrush" Value="#FECACA"/>
            <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
        </Style>
        <Style x:Key="GridRowChipButton" TargetType="Button" BasedOn="{StaticResource GridRowButton}">
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Padding" Value="8,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundBrush}" Margin="32,24,32,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Employees"
                   Style="{StaticResource PageTitle}"
                   Margin="0,0,0,18"/>

        <TabControl x:Name="EmployeeTabs"
                    Grid.Row="1"
                    BorderThickness="0"
                    Background="Transparent">
            <TabItem Header="Employees" Style="{StaticResource PageTabItem}">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="240"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Tag="Search employee..."
                                     VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="1"
                                      ItemsSource="{Binding GroupFilterOptions}"
                                      SelectedItem="{Binding SelectedGroupFilter}"
                                      DisplayMemberPath="Name"
                                      Margin="14,0,0,0"
                                      VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="2"
                                      ItemsSource="{Binding StatusFilters}"
                                      SelectedItem="{Binding SelectedStatusFilter}"
                                      Margin="14,0,0,0"
                                      VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Style="{StaticResource SecondaryButton}"
                                        Content="Import"
                                        Padding="10,7"
                                        Margin="0,0,6,0"
                                        Command="{Binding ImportFromExcelCommand}"/>
                                <Button Style="{StaticResource SecondaryButton}"
                                        Content="Save"
                                        Padding="10,7"
                                        Margin="0,0,6,0"
                                        Command="{Binding SaveChangesCommand}"/>
                                <Button Style="{StaticResource PrimaryButton}"
                                        Content="Add"
                                        Padding="10,7"
                                        Command="{Binding OpenAddEmployeeCommand}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="0"/>
                            <ColumnDefinition Width="0"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
                            <DockPanel>
                                <Grid DockPanel.Dock="Top" Margin="18,14,18,10">
                                    <TextBlock Text="{Binding EmployeeListTitle}" FontSize="17" FontWeight="Bold"/>
                                    <Border HorizontalAlignment="Right" CornerRadius="6" Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1" Padding="10,4">
                                        <TextBlock Text="{Binding Employees.Count, Mode=OneWay, StringFormat={}{0} records}"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </Grid>

                                <Border DockPanel.Dock="Bottom"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="0,1,0,0"
                                        Padding="18,10">
                                    <Grid>
                                        <TextBlock Foreground="{StaticResource MutedTextBrush}">
                                            <Run Text="Showing 1 to "/>
                                            <Run Text="{Binding Employees.Count, Mode=OneWay}"/>
                                            <Run Text=" of "/>
                                            <Run Text="{Binding Employees.Count, Mode=OneWay}"/>
                                            <Run Text=" employees"/>
                                        </TextBlock>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource SecondaryButton}" Content="‹" Padding="12,4" IsEnabled="False"/>
                                            <Button Style="{StaticResource PrimaryButton}" Content="1" Padding="12,4" Margin="8,0"/>
                                            <Button Style="{StaticResource SecondaryButton}" Content="›" Padding="12,4" IsEnabled="False"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <Grid>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20,40">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Employees.Count}" Value="0">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Style="{StaticResource EmptyStateTitle}" Text="No employees found"/>
                                        <TextBlock Style="{StaticResource EmptyStateSubtitle}" Text="Adjust filters or add a new employee."/>
                                    </StackPanel>

                                    <DataGrid ItemsSource="{Binding Employees}"
                                              SelectedItem="{Binding Selected}"
                                              CanUserSortColumns="True"
                                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                                        <DataGrid.Style>
                                            <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Employees.Count}" Value="0">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGrid.Style>
                                        <DataGrid.Columns>
                                            <DataGridTemplateColumn Header="Name" Width="200" MinWidth="180">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <Border Width="28" Height="28" CornerRadius="6"
                                                                    Background="{StaticResource PrimaryBrush}"
                                                                    Margin="0,0,10,0">
                                                                <TextBlock Text="{Binding Name, Converter={StaticResource InitialsConverter}}"
                                                                           Foreground="White"
                                                                           FontWeight="Bold"
                                                                           FontSize="12"
                                                                           HorizontalAlignment="Center"
                                                                           VerticalAlignment="Center"/>
                                                            </Border>
                                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                                <DataGridTemplateColumn.CellEditingTemplate>
                                                    <DataTemplate>
                                                        <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 BorderThickness="0"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellEditingTemplate>
                                            </DataGridTemplateColumn>

                                            <DataGridTextColumn Header="Salary"
                                                                Binding="{Binding BaseSalary, Converter={StaticResource IndianCurrencyConverter}, ConverterParameter=plain2}"
                                                                Width="110"/>
                                            <DataGridTemplateColumn Header="Payment" Width="100" IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Height="30"
                                                                MinWidth="70"
                                                                Padding="8,0"
                                                                Command="{Binding DataContext.CyclePaymentModeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}">
                                                            <Button.Style>
                                                                <Style TargetType="Button" BasedOn="{StaticResource GridRowChipButton}">
                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                    <Setter Property="BorderBrush" Value="#A7F3D0"/>
                                                                    <Setter Property="Foreground" Value="#047857"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding PaymentMode}" Value="Cash">
                                                                            <Setter Property="Background" Value="#F1F5F9"/>
                                                                            <Setter Property="BorderBrush" Value="#CBD5E1"/>
                                                                            <Setter Property="Foreground" Value="#475569"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding PaymentMode}" Value="IciciBank">
                                                                            <Setter Property="Background" Value="#DBEAFE"/>
                                                                            <Setter Property="BorderBrush" Value="#93C5FD"/>
                                                                            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                            <TextBlock FontSize="12" FontWeight="Bold">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Text" Value="Other"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding PaymentMode}" Value="Cash">
                                                                                <Setter Property="Text" Value="Cash"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding PaymentMode}" Value="IciciBank">
                                                                                <Setter Property="Text" Value="ICICI"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Button>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Header="Status" Width="92" IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Border CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" BorderThickness="1">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="#DCFCE7"/>
                                                                    <Setter Property="BorderBrush" Value="#BBF7D0"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                            <Setter Property="Background" Value="#F1F5F9"/>
                                                                            <Setter Property="BorderBrush" Value="#CBD5E1"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <TextBlock FontSize="12" FontWeight="Bold">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Text" Value="Active"/>
                                                                        <Setter Property="Foreground" Value="#166534"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                                <Setter Property="Text" Value="Inactive"/>
                                                                                <Setter Property="Foreground" Value="#64748B"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Border>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Header="Actions" Width="180" IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Button Content="Edit"
                                                                    Style="{StaticResource GridRowButton}"
                                                                    Padding="8,4"
                                                                    Margin="0,0,4,0"
                                                                    Command="{Binding DataContext.OpenEditEmployeeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"/>
                                                            <Button Content="Active"
                                                                    Style="{StaticResource GridRowButton}"
                                                                    Padding="8,4"
                                                                    Margin="0,0,4,0"
                                                                    Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"/>
                                                            <Button Content="Del"
                                                                    Style="{StaticResource GridRowDangerButton}"
                                                                    Padding="8,4"
                                                                    Command="{Binding DataContext.DeleteEmployeeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTextColumn Header="Account No." Binding="{Binding AccountNumber}" Width="170"/>
                                            <DataGridTextColumn Header="IFSC" Binding="{Binding IfscCode}" Width="140"/>
                                            <DataGridTemplateColumn Header="Group" Width="150" IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding GroupMemberships, Converter={StaticResource EmployeeGroupsTextConverter}}"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </DockPanel>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource Card}" Visibility="Collapsed">
                            <DockPanel>
                                <TextBlock DockPanel.Dock="Top" Text="Groups" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                                <Grid DockPanel.Dock="Top" Margin="0,0,0,18">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding NewGroupName, UpdateSourceTrigger=PropertyChanged}" Tag="New group name"/>
                                    <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="Add" Command="{Binding CreateGroupCommand}"/>
                                </Grid>

                                <Button DockPanel.Dock="Bottom"
                                        Style="{StaticResource SecondaryButton}"
                                        Content="Manage Groups"
                                        Margin="0,18,0,0"
                                        Click="ManageGroups_Click"/>

                                <StackPanel VerticalAlignment="Center" Margin="0,24">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding GroupSummaries.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Style="{StaticResource EmptyStateTitle}" Text="No groups yet"/>
                                    <TextBlock Style="{StaticResource EmptyStateSubtitle}" Text="Create a group to organize employees for payroll filters and reports."/>
                                </StackPanel>

                                <ItemsControl ItemsSource="{Binding GroupSummaries}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="{StaticResource BorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="12"
                                                    Margin="0,0,0,8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Width="28" Height="28" CornerRadius="6" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" Margin="0,0,10,0">
                                                        <TextBlock Text="G" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                    <Border Grid.Column="2" CornerRadius="6" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" Padding="8,3">
                                                        <TextBlock Text="{Binding Count}" FontWeight="Bold" Foreground="{StaticResource MutedTextBrush}"/>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DockPanel>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Groups" Style="{StaticResource PageTabItem}">
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="340"/>
                        <ColumnDefinition Width="18"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Group Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                            <TextBlock Text="New group name" Style="{StaticResource FieldLabel}"/>
                            <Grid Margin="0,0,0,18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding NewGroupName, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="Add" Command="{Binding CreateGroupCommand}"/>
                            </Grid>

                            <TextBlock Text="Groups" Style="{StaticResource FieldLabel}"/>
                            <ListBox ItemsSource="{Binding Groups}"
                                     SelectedItem="{Binding SelectedGroup}"
                                     DisplayMemberPath="Name"
                                     Height="180"
                                     Margin="0,0,0,18"/>
                            <StackPanel Margin="0,-8,0,18">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Groups.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <TextBlock Style="{StaticResource EmptyStateTitle}" Text="No groups yet"/>
                                <TextBlock Style="{StaticResource EmptyStateSubtitle}" Text="Add a group name above before assigning employees."/>
                            </StackPanel>

                            <TextBlock Text="Selected group name" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding SelectedGroupName, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding HasSelectedGroup}" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource SecondaryButton}" Content="Rename" Margin="0,0,10,0" IsEnabled="{Binding HasSelectedGroup}" Command="{Binding RenameGroupCommand}"/>
                                <Button Style="{StaticResource SecondaryButton}" Content="Delete" IsEnabled="{Binding HasSelectedGroup}" Command="{Binding DeleteGroupCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0">
                        <DockPanel>
                            <Grid DockPanel.Dock="Top" Margin="20,18,20,14">
                                <TextBlock Text="Employee Assignments" FontSize="18" FontWeight="Bold"/>
                                <Button HorizontalAlignment="Right"
                                        Style="{StaticResource PrimaryButton}"
                                        Content="Save Assignments"
                                        IsEnabled="{Binding HasSelectedGroup}"
                                        Command="{Binding SaveGroupAssignmentsCommand}"/>
                            </Grid>
                            <Grid>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20,40">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasSelectedGroup}" Value="False">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Style="{StaticResource EmptyStateTitle}" Text="Select a group"/>
                                    <TextBlock Style="{StaticResource EmptyStateSubtitle}" Text="Choose or create a group before assigning employees."/>
                                </StackPanel>
                                <ListBox ItemsSource="{Binding GroupAssignmentRows}"
                                         ItemContainerStyle="{StaticResource NeutralChecklistItem}"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         Margin="20,0,20,20"
                                         Padding="0"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         ScrollViewer.CanContentScroll="True"
                                         VirtualizingPanel.IsVirtualizing="True"
                                         VirtualizingPanel.VirtualizationMode="Recycling">
                                    <ListBox.Style>
                                        <Style TargetType="ListBox">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasSelectedGroup}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListBox.Style>
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel/>
                                        </ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="{StaticResource BorderBrush}"
                                                    BorderThickness="0,0,0,1"
                                                    Padding="0,10">
                                                <CheckBox Content="{Binding EmployeeName}"
                                                          IsChecked="{Binding IsMember}"
                                                          FontSize="14"/>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </DockPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
