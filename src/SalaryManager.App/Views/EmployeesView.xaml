<UserControl x:Class="SalaryManager.App.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="PageTabItem" TargetType="TabItem">
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="bd"
                                CornerRadius="8"
                                Background="White"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#EFF6FF"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#BFDBFE"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundBrush}" Margin="32,24,32,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Employees"
                   Style="{StaticResource PageTitle}"
                   Margin="0,0,0,18"/>

        <TabControl x:Name="EmployeeTabs"
                    Grid.Row="1"
                    BorderThickness="0"
                    Background="Transparent">
            <TabItem Header="Employees" Style="{StaticResource PageTabItem}">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="260"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="170"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Tag="Search employee..."
                                     VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="1"
                                      ItemsSource="{Binding GroupFilterOptions}"
                                      SelectedItem="{Binding SelectedGroupFilter}"
                                      DisplayMemberPath="Name"
                                      Margin="14,0,0,0"
                                      VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="2"
                                      ItemsSource="{Binding StatusFilters}"
                                      SelectedItem="{Binding SelectedStatusFilter}"
                                      Margin="14,0,0,0"
                                      VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Style="{StaticResource SecondaryButton}"
                                        Content="Import Excel"
                                        Margin="0,0,10,0"
                                        Command="{Binding ImportFromExcelCommand}"/>
                                <Button Style="{StaticResource SecondaryButton}"
                                        Content="Save Changes"
                                        Margin="0,0,10,0"
                                        Command="{Binding SaveChangesCommand}"/>
                                <Button Style="{StaticResource PrimaryButton}"
                                        Content="Add Employee"
                                        Command="{Binding OpenAddEmployeeCommand}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="18"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
                            <DockPanel>
                                <Grid DockPanel.Dock="Top" Margin="18,14,18,10">
                                    <TextBlock Text="{Binding EmployeeListTitle}" FontSize="17" FontWeight="Bold"/>
                                    <Border HorizontalAlignment="Right" CornerRadius="6" Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1" Padding="10,4">
                                        <TextBlock Text="{Binding Employees.Count, Mode=OneWay, StringFormat={}{0} records}"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </Grid>

                                <Border DockPanel.Dock="Bottom"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="0,1,0,0"
                                        Padding="18,10">
                                    <Grid>
                                        <TextBlock Foreground="{StaticResource MutedTextBrush}">
                                            <Run Text="Showing 1 to "/>
                                            <Run Text="{Binding Employees.Count, Mode=OneWay}"/>
                                            <Run Text=" of "/>
                                            <Run Text="{Binding Employees.Count, Mode=OneWay}"/>
                                            <Run Text=" employees"/>
                                        </TextBlock>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource SecondaryButton}" Content="‹" Padding="12,4" IsEnabled="False"/>
                                            <Button Style="{StaticResource PrimaryButton}" Content="1" Padding="12,4" Margin="8,0"/>
                                            <Button Style="{StaticResource SecondaryButton}" Content="›" Padding="12,4" IsEnabled="False"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <Grid>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20,40">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Employees.Count}" Value="0">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Style="{StaticResource EmptyStateTitle}" Text="No employees found"/>
                                        <TextBlock Style="{StaticResource EmptyStateSubtitle}" Text="Adjust filters or add a new employee."/>
                                    </StackPanel>

                                    <DataGrid ItemsSource="{Binding Employees}"
                                              SelectedItem="{Binding Selected}"
                                              CanUserSortColumns="True"
                                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                                        <DataGrid.Style>
                                            <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Employees.Count}" Value="0">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGrid.Style>
                                        <DataGrid.Columns>
                                            <DataGridTemplateColumn Header="Name" Width="240" MinWidth="220">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <Border Width="28" Height="28" CornerRadius="6"
                                                                    Background="{StaticResource PrimaryBrush}"
                                                                    Margin="0,0,10,0">
                                                                <TextBlock Text="{Binding Name, Converter={StaticResource InitialsConverter}}"
                                                                           Foreground="White"
                                                                           FontWeight="Bold"
                                                                           FontSize="12"
                                                                           HorizontalAlignment="Center"
                                                                           VerticalAlignment="Center"/>
                                                            </Border>
                                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                                <DataGridTemplateColumn.CellEditingTemplate>
                                                    <DataTemplate>
                                                        <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 BorderThickness="0"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellEditingTemplate>
                                            </DataGridTemplateColumn>

                                            <DataGridTextColumn Header="Base Salary (₹)"
                                                                Binding="{Binding BaseSalary, StringFormat=N2}"
                                                                Width="140"/>
                                            <DataGridTemplateColumn Header="Payment Mode" Width="140" IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Height="28"
                                                                Padding="12,0"
                                                                Command="{Binding DataContext.CyclePaymentModeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="BorderThickness" Value="0"/>
                                                                    <Setter Property="Cursor" Value="Hand"/>
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border x:Name="bd"
                                                                                        CornerRadius="6"
                                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                                        Background="{TemplateBinding Background}"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                </Border>
                                                                                <ControlTemplate.Triggers>
                                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                                        <Setter TargetName="bd" Property="Opacity" Value="0.88"/>
                                                                                    </Trigger>
                                                                                </ControlTemplate.Triggers>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                    <Setter Property="BorderBrush" Value="#A7F3D0"/>
                                                                    <Setter Property="BorderThickness" Value="1"/>
                                                                    <Setter Property="Foreground" Value="#047857"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding PaymentMode}" Value="Cash">
                                                                            <Setter Property="Background" Value="#F1F5F9"/>
                                                                            <Setter Property="BorderBrush" Value="#CBD5E1"/>
                                                                            <Setter Property="Foreground" Value="#475569"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding PaymentMode}" Value="IciciBank">
                                                                            <Setter Property="Background" Value="#DBEAFE"/>
                                                                            <Setter Property="BorderBrush" Value="#93C5FD"/>
                                                                            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                            <TextBlock FontSize="12" FontWeight="Bold">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Text" Value="Other"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding PaymentMode}" Value="Cash">
                                                                                <Setter Property="Text" Value="Cash"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding PaymentMode}" Value="IciciBank">
                                                                                <Setter Property="Text" Value="ICICI"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Button>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTextColumn Header="Account No." Binding="{Binding AccountNumber}" Width="170"/>
                                            <DataGridTextColumn Header="IFSC" Binding="{Binding IfscCode}" Width="140"/>
                                            <DataGridTemplateColumn Header="Group" Width="170" IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding GroupMemberships, Converter={StaticResource EmployeeGroupsTextConverter}}"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Header="Status" Width="120" IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Border CornerRadius="6" Padding="10,4" HorizontalAlignment="Left" BorderThickness="1">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="#DCFCE7"/>
                                                                    <Setter Property="BorderBrush" Value="#BBF7D0"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                            <Setter Property="Background" Value="#F1F5F9"/>
                                                                            <Setter Property="BorderBrush" Value="#CBD5E1"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <TextBlock FontSize="12" FontWeight="Bold">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Text" Value="Active"/>
                                                                        <Setter Property="Foreground" Value="#166534"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                                <Setter Property="Text" Value="Inactive"/>
                                                                                <Setter Property="Foreground" Value="#64748B"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Border>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Header="Actions" Width="230" IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Button Content="Edit"
                                                                    Style="{StaticResource SecondaryButton}"
                                                                    Padding="10,4"
                                                                    Margin="0,0,6,0"
                                                                    Command="{Binding DataContext.OpenEditEmployeeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"/>
                                                            <Button Content="Toggle"
                                                                    Style="{StaticResource SecondaryButton}"
                                                                    Padding="10,4"
                                                                    Margin="0,0,6,0"
                                                                    Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"/>
                                                            <Button Content="Delete"
                                                                    Style="{StaticResource DangerButton}"
                                                                    Padding="10,4"
                                                                    Command="{Binding DataContext.DeleteEmployeeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </DockPanel>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource Card}">
                            <DockPanel>
                                <TextBlock DockPanel.Dock="Top" Text="Groups" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                                <Grid DockPanel.Dock="Top" Margin="0,0,0,18">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding NewGroupName, UpdateSourceTrigger=PropertyChanged}" Tag="New group name"/>
                                    <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="Add" Command="{Binding CreateGroupCommand}"/>
                                </Grid>

                                <Button DockPanel.Dock="Bottom"
                                        Style="{StaticResource SecondaryButton}"
                                        Content="Manage Groups"
                                        Margin="0,18,0,0"
                                        Click="ManageGroups_Click"/>

                                <ItemsControl ItemsSource="{Binding GroupSummaries}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="{StaticResource BorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="12"
                                                    Margin="0,0,0,8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Width="28" Height="28" CornerRadius="6" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" Margin="0,0,10,0">
                                                        <TextBlock Text="G" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                    <Border Grid.Column="2" CornerRadius="6" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" Padding="8,3">
                                                        <TextBlock Text="{Binding Count}" FontWeight="Bold" Foreground="{StaticResource MutedTextBrush}"/>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DockPanel>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Groups" Style="{StaticResource PageTabItem}">
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="340"/>
                        <ColumnDefinition Width="18"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Group Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                            <TextBlock Text="New group name" Style="{StaticResource FieldLabel}"/>
                            <Grid Margin="0,0,0,18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding NewGroupName, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="Add" Command="{Binding CreateGroupCommand}"/>
                            </Grid>

                            <TextBlock Text="Groups" Style="{StaticResource FieldLabel}"/>
                            <ListBox ItemsSource="{Binding Groups}"
                                     SelectedItem="{Binding SelectedGroup}"
                                     DisplayMemberPath="Name"
                                     Height="180"
                                     Margin="0,0,0,18"/>

                            <TextBlock Text="Selected group name" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding SelectedGroupName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource SecondaryButton}" Content="Rename" Margin="0,0,10,0" Command="{Binding RenameGroupCommand}"/>
                                <Button Style="{StaticResource SecondaryButton}" Content="Delete" Command="{Binding DeleteGroupCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0">
                        <DockPanel>
                            <Grid DockPanel.Dock="Top" Margin="20,18,20,14">
                                <TextBlock Text="Employee Assignments" FontSize="18" FontWeight="Bold"/>
                                <Button HorizontalAlignment="Right"
                                        Style="{StaticResource PrimaryButton}"
                                        Content="Save Assignments"
                                        Command="{Binding SaveGroupAssignmentsCommand}"/>
                            </Grid>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding GroupAssignmentRows}" Margin="20,0,20,20">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="{StaticResource BorderBrush}"
                                                    BorderThickness="0,0,0,1"
                                                    Padding="0,10">
                                                <CheckBox Content="{Binding EmployeeName}"
                                                          IsChecked="{Binding IsMember}"
                                                          FontSize="14"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
